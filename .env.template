# OrionBelt Runner — environment configuration
# Copy to .env and adjust.

# OBSL REST API base URL (no trailing slash). Defaults to localhost:8080.
OBSL_BASE_URL=http://localhost:8080

# Optional bearer token for OBSL deployments behind auth.
# OBSL_API_TOKEN=

# Default request timeout (seconds).
OBSL_TIMEOUT=30
